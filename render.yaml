services:
  - type: web
    name: livechannels-scraper
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python src/service.py
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
      - key: OUTPUT_DIR
        value: data
      - key: APPEND_TS
        value: "false"
      - key: UPSTREAM_TIMEOUT
        value: "10"
      - key: UPSTREAM_RETRIES
        value: "4"
      - key: LOG_LEVEL
        value: "INFO"
      - key: CHANNELS_JSON
        sync: false
      - key: REMOTE_CONFIG_URL
        sync: false
      - key: REMOTE_CONFIG_TIMEOUT
        value: "15"
